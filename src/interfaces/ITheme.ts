export interface ITheme {
  lightColor: string;
  darkColor: string;
  hoverColor: string;
  textColor: string;
  borderColor: string;
  shadowColor: string;
}

export interface Theme {
  theme: ITheme;
}
